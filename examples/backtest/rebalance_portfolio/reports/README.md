# Reports

Store reusable research reports and normalized markdown outputs here.
Runtime-generated backtest outputs may continue to land in the legacy `results/`
directory until the report pipeline is fully migrated.
